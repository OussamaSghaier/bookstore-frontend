import { TestBed, inject } from '@angular/core/testing';

import { AlertEmiterService } from './alert.emmiter.service.ts.service';

describe('Alert.Emmiter.Service.TsService', () => {
  // beforeEach(() => {
  //   TestBed.configureTestingModule({
  //     providers: [Alert.Emmiter.Service.TsService]
  //   });
  // });
  //
  // it('should be created', inject([Alert.Emmiter.Service.TsService], (service: Alert.Emmiter.Service.TsService) => {
  //   expect(service).toBeTruthy();
  // }));
});
